version: "3.8"
services:
  copilot-api:
    container_name: copilot-api
    build: .
    ports:
      - "4141:4141"
    env_file: .env
    volumes:
      - copilot-data:/root/.local/share/copilot-api
    restart: unless-stopped

volumes:
  copilot-data:
